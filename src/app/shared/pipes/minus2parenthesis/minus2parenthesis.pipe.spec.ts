import { Minus2parenthesisPipe } from './minus2parenthesis.pipe';

describe('Minus2parenthesisPipe', () => {
  it('create an instance', () => {
    const pipe = new Minus2parenthesisPipe();
    expect(pipe).toBeTruthy();
  });
});
